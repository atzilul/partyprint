import type {Order} from './orders';
export type PrintSpec={side:'front'|'back';placement:string;widthCm:number;heightCm:number;imageKey:string;shirtNumbers:number[]};
export type Workflow={assignee?:string;task?:string;taskDue?:string;taskDone?:boolean;eventDate?:string;archivedAt?:string;cancelledAt?:string;closeReason?:string;printSpecs?:PrintSpec[];printNote?:string;source?:string;customerEmailStatus?:string;stageHistory?:{status:string;at:string}[]};
export function validatePrintSpecs(value:unknown,o:Order):PrintSpec[]{if(!Array.isArray(value)||value.length>200)throw Error('invalid');return value.map(v=>{if(!v||!['front','back'].includes(v.side)||typeof v.placement!=='string'||!v.placement.trim()||v.placement.length>150||typeof v.widthCm!=='number'||v.widthCm<=0||v.widthCm>60||typeof v.heightCm!=='number'||v.heightCm<=0||v.heightCm>90||!o.images.some(i=>i.key===v.imageKey&&(i.role==='design'||i.role==='print'))||!Array.isArray(v.shirtNumbers)||!v.shirtNumbers.length||new Set(v.shirtNumbers).size!==v.shirtNumbers.length||v.shirtNumbers.some((n:unknown)=>!Number.isInteger(n)||Number(n)<1||Number(n)>o.quantity))throw Error('invalid');return {side:v.side,placement:v.placement.trim(),widthCm:v.widthCm,heightCm:v.heightCm,imageKey:v.imageKey,shirtNumbers:v.shirtNumbers}})}
export function stageMessage(o:Order){const base=`היי ${o.name}, כאן PARTYPRINT לגבי הזמנה ${o.id.slice(0,8).toUpperCase()}. `;const messages:Record<string,string>={received:'הבקשה שלכם התקבלה! נשמח לתאם את הרעיון, המידות ומועד האירוע.',designing:'אנחנו עובדים על העיצוב שלכם. יש עוד פרט או תמונה שחשוב שנכיר?',review:'העיצוב מחכה לתשובתכם בקישור האישור ששלחנו. נשמח לשמוע מה דעתכם.',approved:'תודה על אישור העיצוב! מתאמים את הפרטים האחרונים לקראת ההדפסה.',printing:'החולצות שלכם בהדפסה. נעדכן כשהן מוכנות.',printed:'החולצות מוכנות! נרצה לוודא את כתובת המשלוח: '+(o.address||'מה הכתובת למשלוח?'),shipped:'החולצות נשלחו! '+(o.tracking?'פרטי המעקב: '+o.tracking:'נעדכן בפרטי המעקב.')};return base+(messages[o.status]||messages.received)}
