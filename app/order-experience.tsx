"use client";
import {useEffect,useState} from 'react';import type {SiteContent} from '@/lib/site-content';
export function usePublicContent(){const [c,setC]=useState<SiteContent>({version:0,sizes:[],gallery:[],leadDays:null});useEffect(()=>{fetch('/api/content').then(r=>r.json() as Promise<SiteContent>).then(setC).catch(()=>{})},[]);return c}
export function SizeGuide(){const c=usePublicContent();return <details className="size-guide"><summary>איך בוחרים מידה? מדריך מדידה</summary><p>הניחו חולצה שמתאימה לכם על משטח ישר, בלי למתוח אותה. מדדו רוחב בין בתי השחי ואורך מהנקודה הגבוהה בכתף עד המכפלת.</p><div className="measure-labels"><span>↔ רוחב: בין בתי השחי</span><span>↕ אורך: כתף עד מכפלת</span></div>{c.sizes.length?<table><caption>מידות החולצה בסנטימטרים · לפי נתוני הסטודיו</caption><thead><tr><th>מידה</th><th>רוחב</th><th>אורך</th></tr></thead><tbody>{c.sizes.map(s=><tr key={s.size}><th>{s.size}</th><td>{s.width}</td><td>{s.length}</td></tr>)}</tbody></table>:<p>טבלת המידות של הדגם עדיין לא פורסמה. שלחו לנו את שתי המדידות ונעזור בהתאמה לפני אישור ההזמנה.</p>}<p>הבחירה באתר היא S–XL. להזמנות לילדים נוודא יחד התאמה; סימון S אינו מבטיח מידת ילדים.</p><a href="https://wa.me/972552896236?text=אשמח%20לעזרה%20בהתאמת%20מידה" target="_blank" rel="noreferrer">עזרה בבחירת מידה ↗</a></details>}
export function EventDate({value,onChange,error}:{value:string;onChange:(v:string)=>void;error?:string}){const c=usePublicContent(),today=new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Jerusalem'}),days=value?Math.ceil((Date.parse(value)-Date.parse(today))/86400000):null;return <div className={"event-date "+(error?"field-missing":"")}><label>מתי האירוע? <small>(עוזר לנו לבדוק זמינות)</small><input type="date" name="eventDate" aria-invalid={!!error} aria-describedby={error?"event-date-error":"event-date-help"} value={value} min={today} onChange={e=>onChange(e.target.value)}/></label>{error&&<span className="missing-hint" id="event-date-error" role="alert">{error}</span>}{value&&!error&&<p role="status">{days!==null&&days<0?'בחרו תאריך מהיום והלאה.':c.leadDays!==null&&days!==null&&days<c.leadDays?'האירוע קרוב מזמן התכנון הרגיל שלנו. נבדוק זמינות אישית לפני אישור ההזמנה.':'נבדוק את מועד האספקה מול תאריך האירוע ונאשר אותו איתכם.'}</p>}<small id="event-date-help">התאריך לא מחייב הזמנה. נבדוק זמינות ומועד אספקה לפני שמתקדמים.</small></div>}
export function RealGallery(){const c=usePublicContent();if(!c.gallery.length)return null;return <section className="section wrap real-gallery"><div className="section-head"><div className="eyebrow">MADE FOR REAL PEOPLE</div><h2>מהסטודיו. לחגיגות שלכם.</h2><p>צילומי עבודות שפורסמו באישור הלקוחות.</p></div><div className="real-grid">{c.gallery.map(i=><article key={i.id}><img loading="lazy" src={'/api/content?image='+i.id} alt={i.title}/><h3>{i.title}</h3>{i.quote&&<><blockquote>״{i.quote}״</blockquote><p>{i.customer}</p></>}</article>)}</div></section>}
